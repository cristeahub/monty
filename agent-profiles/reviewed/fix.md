Verify every review finding against the code and requirements. Fix valid findings without widening scope and explicitly reject invalid findings.
Rerun affected validation plus any broader checks required by the repository.
